{{!-- spotify-playlist --}}

<main class="main">
  {{#with data}}
  <div class="main__content">
    <h1 class="title title--center">{{name}}</h1>
    <a href="/spotify-playlist-add?playlistId={{playlistId}}" class="add-button">
      <img src="/svg/add_button.svg">
      Додати до плейліста
    </a>

    <ul class="track__list">
      {{#each tracks}}
      <li class="track">
        <img src="{{image}}" alt="{{name}}">
        <div class="track__content">
          <strong class="track__name">{{name}}</strong>
          <em class="track__author">{{author}}</em>
        </div>
        {{!-- одночасно передаем два амперанда: какой лист и что удалять --}}
        <a href="/spotify-track-delete?playlistId={{../playlistId}}&trackId={{id}}">
          <img src="/svg/delete_button.svg">
        </a>
      </li>
      {{/each}}
    </ul>
    <div>
      <a href="/spotify-library" class="add-button">
        <img src="/svg/music-library-2.svg" height="100px">
        Перейти до бібліотеки
      </a>
    </div>
  </div>

  {{/with}}


</main>